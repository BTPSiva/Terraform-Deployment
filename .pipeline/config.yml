stages:
  - name: Init

steps:
  Init:
    runSh:
      shell: bash
      script: |
        echo "Verifying Terraform installation..."
        terraform version || echo "Terraform is NOT installed"
    
