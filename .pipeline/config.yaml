---
general:
  buildTool: "mta"
service:
  buildToolVersion: "MBTJ21N22"
  stages:
    Acceptance:
      cfCredentialsId: "terraformdeployment"
stages:
  Build:
    mavenExecuteStaticCodeChecks: false
    npmExecuteLint: false
  Acceptance:
    cfApiEndpoint: "https://api.cf.us10-001.hana.ondemand.com"
    cfOrg: "f4437109trial_devops-su14zqmj"
    cfSpace: "dev"
    deployType: "standard"
    cloudFoundryDeploy: true
    npmExecuteEndToEndTests: false
  Malware Scan:
    malwareExecuteScan: false
  Release:
    tmsExport: false
    tmsUpload: false
    cloudFoundryDeploy: false
  Additional Unit Tests:
    npmExecuteScripts: false
  Compliance:
    sonarExecuteScan: false
steps:
  cloudFoundryDeploy:
    mtaDeployParameters: "-f --version-rule ALL"
  artifactPrepareVersion:
    versioningType: "cloud_noTag"

# Stages configuration
stages:
  Init:

steps:
  Init:
      script: |
    #set BTP_USERNAME=perabathula.nagasiva@sap.com
    #set BTP_PASSWORD=Allgodsofmexico27
    echo Terraform code is started deploying.
    echo executing the Terraform code in SAP BTP.......
    terraform --version
    terraform init
    terraform plan


  
